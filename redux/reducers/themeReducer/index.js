import initialState from "../../constants/InitialStateTheme";

const themeReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default themeReducer;
